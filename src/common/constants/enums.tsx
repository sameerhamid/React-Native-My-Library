// Alert Style

export enum AlertStyle {
  cancel = 'cancel',
  default = 'default',
  destructive = 'destructive',
}

// Style of Border

export enum BorderStyle {
  DOTTED = 'dotted',
  DASHED = 'dashed',
  SOLID = 'solid',
}

// Type of border

export enum BorderType {
  DOTTED = 1,
  SOLID = 2,
  DASHED = 3,
}
